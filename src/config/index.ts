export const acceptedAvatarType: string[] = ['image/jpeg', 'image/jpg', 'image/png'];

export const avartarMaxSize = 500 * 1000; // in kb

export const avatarsStoragePath = 'avatars';

export const languages = ['en', 'zh', 'es'];

export const defaultLang = languages[0];

export const currency = '$';

export const siteTitle = 'Universal Music Group Shop';
